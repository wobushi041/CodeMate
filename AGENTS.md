# 项目级 Agent 规则

## 回复语言

- Always reply in Chinese.
- 除非用户明确要求英文，否则所有回复使用简体中文。
- 代码标识符、命令、日志、报错信息保持原始语言；其余解释用中文。
- 如果不小心用英文作答，立即用中文重写同一答案，不要只追加一句中文。
- 用户系统为 Windows 时，优先使用 Windows 原生支持的终端命令与 PowerShell；仅在 Windows 命令不适用或用户明确要求时再考虑 bash。

## 当前工作阶段：前端样式修复

- 当前阶段只处理前端样式相关问题，包括页面布局、间距、高度、溢出、导航栏/标签栏展示、颜色、对齐和响应式表现。
- 接到样式修复任务时，先做与问题直接相关的代码排查，再进行最小范围代码修改。
- 不主动扩展到后端逻辑、接口协议、数据模型、依赖升级、业务流程重构或无关页面重构。
- 不回滚用户已有改动；若发现无关改动，仅记录并避开。

## 修改边界

- 优先修改 `matchsystem-frontend/src` 下与问题直接相关的 Vue、CSS、布局或样式配置文件。
- 保持现有技术栈和代码风格，优先沿用项目已有的 Vue、Vant、Tailwind、CSS 变量和页面结构。
- 对样式问题做局部修复，避免引入新的组件体系或大范围重写。
- 只在确实影响当前样式问题时调整公共布局文件。

## 检查规则

- 当前阶段不要在修改后主动执行静态检查、构建检查、浏览器检查、Playwright 检查、截图检查或自动化 UI 验证。
- 不主动运行 `npm run build`、`npm run dev`、`vue-tsc`、lint、test、browser automation 等验证命令，除非用户明确要求。
- 修改后只做代码 review：说明改了哪些文件、为什么这样改、可能影响哪些页面、是否存在需要用户手动确认的视觉点。
- 如果用户要求验证，再按用户指定范围执行对应命令。

## 汇报方式

- 回复保持简洁，优先说明变更点和代码层面的影响。
- 对样式问题用中文描述原因，保留类名、文件名、组件名、命令和报错的原始语言。
- 若存在无法仅通过代码 review 判断的视觉效果，直接说明需要用户打开页面确认，不主动启动服务或截图验证。
